name: PR Verifier

permissions: {}

on:
  pull_request:
    types: [ opened, edited, reopened, synchronize, ready_for_review ]

jobs:
  verify:
    name: verify PR contents
    uses: metal3-io/project-infra/.github/workflows/pr-verifier.yaml@f3c3ae367c63cada0274fc96ee6c0a1abd28fa97 # main
