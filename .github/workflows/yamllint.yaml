name: yamllint

permissions:
  contents: read

on:
  pull_request:
    types: [ opened, edited, reopened, synchronize, ready_for_review ]

jobs:
  yamllint-check:
    name: yaml-lint
    uses: metal3-io/project-infra/.github/workflows/yamllint.yaml@f3c3ae367c63cada0274fc96ee6c0a1abd28fa97 # main
    with:
      ref: ${{ github.event.pull_request.head.sha }}
